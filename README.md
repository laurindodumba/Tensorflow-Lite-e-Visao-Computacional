## Mobile Computer anf TensorFlow Lite

This project is a mobile computer vision application built using Kotlin and TensorFlow Lite, designed to detect and classify objects in real time directly on mobile devices.

The goal is to provide a lightweight, fast, and efficient AI solution that runs entirely on-device without requiring internet connectivity
